package com.reactor.reactor.services;

import com.reactor.reactor.models.Client;

public interface IClientService extends ICRUD<Client, String> {

    // método
    // Mono<T> diferentMethod(ID id, T t);

}
